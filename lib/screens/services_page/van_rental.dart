import 'package:flutter/material.dart';

class VanRental extends StatelessWidget {
  const VanRental({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Van Rental"),
    );
  }
}
